<!DOCTYPE html>
<html>

<head>
	<title>Tourist Spots</title>
	<style>
		body {
			font-family: Arial, sans-serif;
			margin: 0;
			padding: 0;
		}

		header {
			background-color: #333;
			color: #fff;
			padding: 10px;
			text-align: center;
		}

		h1 {
			margin: 0;
		}

		ul {
			list-style: none;
			margin: 0;
			padding: 0;
		}

		li {
			border: 1px solid #ccc;
			margin-bottom: 10px;
			padding: 10px;
		}

		img {
			max-width: 100%;
		}

		h3 {
			margin: 0;
			margin-bottom: 10px;
		}

		p {
			margin: 0;
		}

		.date {
			font-style: italic;
		}
	</style>
</head>

<body>
	<header>
		<h1>Tourist Spots</h1>
	</header>
	<main>
		<% if (touristSpots.length> 0) { %>
			<ul>
				<% for (let i=0; i < touristSpots.length; i++) { %>
					
							<li>
								<h3>
									<%= touristSpots[i].name %>
								</h3>
								<img src="<%= touristSpots[i].image %>" alt="<%= touristSpots[i].name %> Image">
								<p>
									<%= touristSpots[i].description %>
								</p>
								<p>Rating: <%= touristSpots[i].rating %> out of 5</p>
								<p class="date">Date: <%= touristSpots[i].date %>
								</p>
							</li>
							<% } %>
			</ul>
			<% } else { %>
				<p>No tourist spots found.</p>
				<% } %>
	</main>
	<!-- <script>
		function calculateTravelTime(origin, destination) {
			const distance = getDistance(origin, destination); // assume you have a getDistance() function that calculates the distance between two tourist spots
			const speed = 60; // assume an average speed of 60 km/h
			return Math.round(distance / speed); // round to nearest hour
		}

		function getDistance(lat1, lon1, lat2, lon2) {
			const R = 6371; // Radius of the earth in km
			const dLat = deg2rad(lat2 - lat1);
			const dLon = deg2rad(lon2 - lon1);
			const a =
				Math.sin(dLat / 2) * Math.sin(dLat / 2) +
				Math.cos(deg2rad(lat1)) * Math.cos(deg2rad(lat2)) *
				Math.sin(dLon / 2) * Math.sin(dLon / 2);
			const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
			const d = R * c; // Distance in km
			return d;
		}

		function deg2rad(deg) {
			return deg * (Math.PI / 180)
		}

	</script> -->
</body>

</html>